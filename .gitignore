# Hugo build output (CI rebuilds it)
public/
resources/_gen/
.hugo_build.lock

# VPS ops live in a separate private repo — never in this public one
infra/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*~

# Local state
node_modules/

# Dashboard data is regenerated on demand — don't commit
tools/data.js

# Agent / editor scratch (holds local hostnames and permissions)
.claude/

# Throwaway scratch files
/a
/a.cpp
/.cpp
/Untitled
.nvimlog
.codex-*.png
