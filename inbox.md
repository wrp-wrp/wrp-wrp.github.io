# Inbox — quick captures

Anything you want to write about, fix, or remember. Newest at top.
Use `scripts/blog-workflow.sh inbox "your idea"` to append a line.
Mark with `- [x]` to keep as a record without showing as pending.

- [ ] (example) write a short note on streaming HNSW recall regression
